// Dependency injection for transactions module
void initTransactions() {
  // No providers or dependencies specific to transactions currently
  // This module uses DashboardProvider from dashboard module
}
